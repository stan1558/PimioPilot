# Copy to .env and fill in your local values. .env is already gitignored.
DB_HOST=timescaledb
DB_PORT=5432
DB_NAME=marketdata
DB_USER=__CHANGEME__
DB_PASSWORD=__CHANGEME__

# Optional: you can provide a full DSN instead of the above discrete fields
# DB_DSN=postgresql://USER:PASSWORD@HOST:5432/marketdata
